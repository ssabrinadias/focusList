import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles(() => ({
  root: {
    marginTop: 60,
    marginBottom: 16,
    padding: 16,
    minWidth: "340px",
  },
  button: {
    marginTop: 16,
  },
}));
